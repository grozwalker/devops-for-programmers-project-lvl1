version: "3.9"

services:
  app:
    image: grozwalker/devops-for-programmers-project-lvl1:latest
    command: npm test
    depends_on:
      - db

  db:
    image: postgres:14-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: postgres
